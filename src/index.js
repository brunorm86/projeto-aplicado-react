import React from "react";
import ReactDOM from "react-dom";
import "./index.css";
// import App from "./App";
import { Estado } from "./components/Estado";

ReactDOM.render(<Estado />, document.getElementById("root"));

// Durante a aula, nós usamos o componente "Direcao".
// Estou comentando este componente App, pois vamos usá-lo para criar nossa app do Pokemon

// ReactDOM.render(<App />,
//   document.getElementById('root')
// );
